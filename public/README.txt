# public/ 目录说明

你需要在这里放两个文件：

## 1. target.mind
- 这是图像识别目标
- 用 https://hiukim.github.io/mind-ar-js-doc/tools/compile 把你的标识图（PNG/JPG）转换得到
- 放在这里，文件名必须是 target.mind

## 2. prototype.glb
- 这是你的 3D 模型
- 从 Blender 导出：File → Export → glTF 2.0 (.glb)
- 放在这里，文件名必须是 prototype.glb

详见 ../README.md
